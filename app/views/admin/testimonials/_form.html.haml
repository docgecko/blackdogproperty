= form_for @admin_testimonial do |f|
  -if @admin_testimonial.errors.any?
    #error_explanation
      %h2= "#{pluralize(@admin_testimonial.errors.count, "error")} prohibited this admin_testimonial from being saved:"
      %ul
        - @admin_testimonial.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
