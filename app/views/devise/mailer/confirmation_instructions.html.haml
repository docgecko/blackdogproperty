%p== Hello #{(@resource.first_name.blank? and @resource.last_name.blank?) ? @resource.email : [@resource.first_name, " ", @resource.last_name].join.strip}!

%p Please confirm your BlackDog Property account by clicking the link below or by copying and pasting the URL into your browser:

%p= link_to confirmation_url(@resource, :confirmation_token => @resource.confirmation_token), confirmation_url(@resource, :confirmation_token => @resource.confirmation_token)

%p Once you confirm, you will have full access to create and administer your properties adverts, and all future notifications will be sent to this email address.

%p Welcome to BlackDog Property!

%p The BlackDog Property Team